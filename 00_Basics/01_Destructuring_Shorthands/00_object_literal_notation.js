// CODE BLOCK #1
const user1 = {};
user1.name = 'Irfan Can';
user1.handle = '@irfancnk';
user1.location = 'Ankara, Turkey';

// CODE BLOCK #2
const user2 = {
    name: 'Irfan Can',
    handle: '@irfancnk',
    location: 'Ankara, Turkey'
};

console.log(user1);
console.log(user2);

// Two code blocks are actually identical. If we want to add multiple properties
// to an object at the same time, we use object literal notation.
