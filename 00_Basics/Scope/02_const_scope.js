function applyDiscount(prices, percentage) {
    var discounted = [];
    for (let i = 0; i < prices.length; i++) {
        const assignOnce = 1;
    }
    try {
        console.log(assignOnce);
    } catch (e) {
        console.log(e);
    }
    return discounted;
}

applyDiscount([100], 0.2);

// Since variables declared with const are block scoped, they are not accessible
// outside the for loop. const is scoped to the nearest enclosing block.
